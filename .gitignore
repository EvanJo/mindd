# wiki/ is a symlink to the Obsidian vault — each machine recreates it via setup.sh
wiki

# qmd search index
index.sqlite

# scratch/test data (large binaries, extraction artifacts)
test/

# OS
.DS_Store
